import 'package:flutter/cupertino.dart';

class UsersFolow extends StatelessWidget {
  const UsersFolow({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('folow');
  }
}
