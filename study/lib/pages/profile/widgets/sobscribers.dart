import 'package:flutter/material.dart';

class Sobscribers extends StatelessWidget {
  const Sobscribers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Подписки');
  }
}
